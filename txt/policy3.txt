@KnowledgeBase
    R1 :: dist(X), ?checkFar(X) implies blinkLED1slow;
    R2 :: dist(X), ?checkClose(X) implies blinkLED1fast;
    C1 :: blinkLED1fast # blinkLED1slow;
    R3 :: btn1_pressed implies sysStandby;
    C2 :: blinkLED1slow # sysStandby;
    C3 :: blinkLED1fast # sysStandby;
  
@Code
function checkFar(x) {
    return parseFloat(x) > 10;
}
function checkClose(x) {
    return parseFloat(x) < 10;
}
